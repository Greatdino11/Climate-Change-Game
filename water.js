/*class Water {
    constructor(x,y,width,height) {
      this.width = width;
      this.height = height;
      this.x = x;
      this.y = y;
    }
    display(){
      fill("brown");
      rect(x, y, this.width, this.height);
    }
  };*/
